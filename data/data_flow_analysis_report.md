# รายงานวิเคราะห์ Data Flow และสาเหตุที่ข้อมูลเปลี่ยนไป

**วันที่วิเคราะห์:** 2026-02-13

---

## 🔍 สรุปปัญหา

**คำถาม:** ทำไมข้อมูลเปลี่ยนไปเมื่อปรับ logic? ถ้าระบบดึงข้อมูลมาก่อนแล้วค่อยกรอง ข้อมูลไม่ควรหายไป

**คำตอบ:** ข้อมูลไม่ได้หายไป แต่ **trades ที่บันทึกลง trade_history เปลี่ยนไป** เพราะ gatekeeper ใน backtest.py เปลี่ยน

---

## 📋 Data Flow ของระบบ

### 1. **Data Fetching (ดึงข้อมูล)**
```
backtest.py → get_data_with_cache() → ดึงข้อมูล 5000 bars
```
- ✅ **ไม่มีการกรอง** - ดึงข้อมูลทั้งหมดมา
- ✅ **ข้อมูลไม่หายไป** - ข้อมูลครบ 5000 bars

### 2. **Pattern Recognition (คำนวณ Pattern)**
```
backtest.py → คำนวณ pattern จากข้อมูลทั้งหมด
```
- ✅ **ไม่มีการกรอง** - คำนวณ pattern ทุกตัว
- ✅ **ข้อมูลไม่หายไป** - Pattern stats ครบทุกตัว

### 3. **Gatekeeper (กรองก่อนบันทึก)** ⚠️ **จุดที่เปลี่ยน**
```
backtest.py → Gatekeeper (Prob >= 53%) → กรอง trades
```
- ⚠️ **มีการกรอง** - Trades ที่ Prob < 53% จะไม่ถูกบันทึก
- ⚠️ **Trades ที่บันทึกเปลี่ยน** - ขึ้นอยู่กับ gatekeeper

### 4. **บันทึกลง trade_history**
```
backtest.py → save_trade_logs() → บันทึก trades ที่ผ่าน gatekeeper
```
- ⚠️ **Trades ที่บันทึกเปลี่ยน** - ขึ้นอยู่กับ gatekeeper
- ⚠️ **Prob field ถูกบันทึก** - Prob% จาก pattern matching

### 5. **calculate_metrics (อ่าน trade_history)**
```
calculate_metrics.py → อ่าน trade_history → Elite Filter (Prob >= 60%)
```
- ⚠️ **Elite Filter** - กรอง trades ที่ prob >= 60%
- ⚠️ **ถ้าไม่มี Elite Trades** - ใช้ Raw Prob% แทน

---

## 🔴 ปัญหาของ TPIPP

### ข้อมูล TPIPP ใน trade_history:
- **จำนวน trades:** 75 trades
- **Prob% Range:** 53.33% - 54.55% (ทั้งหมดอยู่ในช่วง 53-55%)
- **Prob% สูงสุด:** 54.5% ❌ (ต่ำกว่า 60%)
- **Raw Prob%:** 45.3% (Correct: 34/75)

### สาเหตุที่ Elite Count = 0:
1. **Prob% ใน trade_history ต่ำ** - สูงสุด 54.5% (ต่ำกว่า 60%)
2. **Elite Filter กรองออกหมด** - ไม่มี trades ไหนที่มี prob >= 60%
3. **ใช้ Raw Prob% แทน** - 45.3% (ต่ำกว่า 60%)

---

## 📊 การเปลี่ยนแปลง Gatekeeper

### Gatekeeper เดิม (V9.0 หรือก่อนหน้า):
```python
# Thai Market
min_prob = 55.0  # หรือ 60.0
if cand_prob < min_prob:
    continue  # ไม่บันทึก
```

**ผลลัพธ์:**
- Trades ที่ Prob >= 55% (หรือ 60%) ถูกบันทึก
- Prob% ใน trade_history อาจสูงกว่า (55-65%)
- Elite Filter อาจเจอ trades ที่ prob >= 60%

### Gatekeeper ปัจจุบัน (V10.1):
```python
# Thai Market
min_prob = 53.0  # V10.1: ลดจาก 55% → 53%
if cand_prob < min_prob:
    continue  # ไม่บันทึก
```

**ผลลัพธ์:**
- Trades ที่ Prob >= 53% ถูกบันทึก
- Prob% ใน trade_history ต่ำกว่า (53-55%)
- Elite Filter ไม่เจอ trades ที่ prob >= 60% → Elite Count = 0

---

## 💡 สาเหตุที่ข้อมูลเปลี่ยนไป

### 1. **Gatekeeper เปลี่ยน → Trades ที่บันทึกเปลี่ยน**

**เดิม:**
- Gatekeeper: Prob >= 55% (หรือ 60%)
- Trades ที่บันทึก: Prob >= 55%
- Prob% ใน trade_history: 55-65%

**ปัจจุบัน:**
- Gatekeeper: Prob >= 53%
- Trades ที่บันทึก: Prob >= 53%
- Prob% ใน trade_history: 53-55%

**ผลกระทบ:**
- Trades เก่าถูกแทนที่ด้วย trades ใหม่
- Prob% ใน trade_history ลดลง
- Elite Filter ไม่เจอ trades ที่ prob >= 60%

### 2. **Elite Filter ทำงานหลังบันทึก**

**Flow:**
1. backtest.py → Gatekeeper (Prob >= 53%) → บันทึกลง trade_history
2. calculate_metrics.py → อ่าน trade_history
3. Elite Filter (Prob >= 60%) → กรอง trades
4. ถ้าไม่มี Elite Trades → ใช้ Raw Prob%

**ปัญหา:**
- Gatekeeper ต่ำ (53%) → Prob% ใน trade_history ต่ำ (53-55%)
- Elite Filter สูง (60%) → ไม่เจอ trades
- → Elite Count = 0 → ใช้ Raw Prob% (ต่ำ)

### 3. **Prob% ใน trade_history ถูกกำหนดโดย Gatekeeper**

**สำคัญ:**
- Prob% ใน trade_history = Prob% จาก pattern matching
- แต่ Prob% ที่บันทึก = Prob% ของ trades ที่ผ่าน gatekeeper
- ถ้า gatekeeper ต่ำ → Prob% ที่บันทึกต่ำ

---

## 🔍 ตัวอย่าง: TPIPP

### ข้อมูลจริง:
- **Prob% ใน trade_history:** 53.33% - 54.55%
- **Raw Prob% (Correct):** 45.3% (34/75)
- **Elite Count:** 0 (ไม่มี trades ที่ prob >= 60%)

### ทำไม Prob% ใน trade_history ต่ำ?
1. **Gatekeeper ต่ำ (53%)** → Trades ที่ Prob 53-55% ถูกบันทึก
2. **Pattern matching ให้ Prob% ต่ำ** → Prob% จาก pattern = 53-55%
3. **Elite Filter สูง (60%)** → ไม่เจอ trades

### ทำไม Raw Prob% ต่ำ (45.3%)?
- **Correct:** 34/75 = 45.3%
- **หมายความว่า:** Trades ที่ผ่าน gatekeeper (Prob >= 53%) แต่จริงๆ แล้วชนะแค่ 45.3%
- **ปัญหา:** Gatekeeper ต่ำเกินไป → Trades ที่ Prob% สูงแต่จริงๆ แล้วไม่ดีถูกบันทึก

---

## 📊 สรุป Data Flow

```
1. ดึงข้อมูล (5000 bars) ✅ ไม่มีการกรอง
   ↓
2. คำนวณ Pattern ✅ ไม่มีการกรอง
   ↓
3. Gatekeeper (Prob >= 53%) ⚠️ กรองก่อนบันทึก
   ↓
4. บันทึกลง trade_history (พร้อม prob field)
   ↓
5. calculate_metrics อ่าน trade_history
   ↓
6. Elite Filter (Prob >= 60%) ⚠️ กรองอีกครั้ง
   ↓
7. ถ้า Elite Count >= 5 → ใช้ Elite Prob%
   ถ้า Elite Count < 5 → ใช้ Raw Prob%
```

---

## ⚠️ ปัญหาหลัก

### 1. **Gatekeeper ต่ำเกินไป**
- Gatekeeper: Prob >= 53%
- Prob% ใน trade_history: 53-55%
- Elite Filter: Prob >= 60%
- → ไม่เจอ Elite Trades

### 2. **Elite Filter สูงเกินไป**
- Elite Filter: Prob >= 60%
- Prob% ใน trade_history: 53-55%
- → Elite Count = 0

### 3. **Raw Prob% ต่ำ**
- Raw Prob%: 45.3%
- หมายความว่า: Trades ที่ผ่าน gatekeeper แต่จริงๆ แล้วไม่ดี
- → ไม่ผ่านเกณฑ์ Prob >= 60%

---

## 💡 คำแนะนำ

### 1. **ปรับ Gatekeeper ให้สูงขึ้น**
- เพิ่มจาก Prob >= 53% → Prob >= 55% หรือ 60%
- → Prob% ใน trade_history จะสูงขึ้น
- → Elite Filter อาจเจอ trades

### 2. **ปรับ Elite Filter ให้ต่ำลง**
- ลดจาก Prob >= 60% → Prob >= 55%
- → Elite Filter อาจเจอ trades
- → ใช้ Elite Prob% แทน Raw Prob%

### 3. **ตรวจสอบ Pattern Matching**
- ทำไม Prob% จาก pattern matching ต่ำ (53-55%)?
- Pattern matching ทำงานถูกต้องหรือไม่?
- ควรปรับ threshold หรือ logic หรือไม่?

---

## 📝 หมายเหตุ

- **ข้อมูลไม่หายไป** - ข้อมูลครบ 5000 bars
- **แต่ trades ที่บันทึกเปลี่ยน** - ขึ้นอยู่กับ gatekeeper
- **Prob% ใน trade_history เปลี่ยน** - ขึ้นอยู่กับ gatekeeper
- **Elite Filter ทำงานหลังบันทึก** - ไม่ได้กรองตอนดึงข้อมูล

---

## 🔄 สรุป

**คำถาม:** ทำไมข้อมูลเปลี่ยนไปเมื่อปรับ logic?

**คำตอบ:**
1. ✅ ข้อมูลไม่หายไป - ข้อมูลครบ 5000 bars
2. ⚠️ แต่ trades ที่บันทึกลง trade_history เปลี่ยนไป
3. ⚠️ เพราะ gatekeeper ใน backtest.py เปลี่ยน (Prob >= 53%)
4. ⚠️ Prob% ใน trade_history ต่ำ (53-55%) → Elite Filter ไม่เจอ
5. ⚠️ Elite Count = 0 → ใช้ Raw Prob% (45.3%) → ไม่ผ่านเกณฑ์

**วิธีแก้:**
- ปรับ gatekeeper ให้สูงขึ้น (Prob >= 55% หรือ 60%)
- หรือปรับ Elite Filter ให้ต่ำลง (Prob >= 55%)
- หรือตรวจสอบ pattern matching logic

