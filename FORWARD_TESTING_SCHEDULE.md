# 📅 Forward Testing Schedule - เมื่อไหร่ควรรัน?

## 🔍 สถานการณ์ปัจจุบัน

### ❌ **ปัญหาที่พบ:**

**โค้ดปัจจุบัน (`core/performance.py`):**
```python
today = datetime.now().strftime('%Y-%m-%d')
tomorrow = (datetime.now() + timedelta(days=1)).strftime('%Y-%m-%d')
target_date = tomorrow
```

**ปัญหาคือ:**
- ถ้าวันศุกร์เย็นรัน → `target_date = วันเสาร์` (แต่ตลาดปิด)
- ถ้าวันจันทร์เย็นรัน → `target_date = วันอังคาร` (ถูกต้อง)

---

## ✅ **คำตอบ: ควรรันวันนี้ตอนเย็น (วันศุกร์)**

### 📊 **เหตุผล:**

1. **วันศุกร์เย็นรัน:**
   - ระบบจะทำนาย `target_date = วันเสาร์` (แต่ตลาดปิด)
   - `verify_forecast()` จะ verify เมื่อ `target_date <= today` และตลาดปิด
   - ถ้าวันจันทร์เย็นรัน → จะ verify forecast ที่ `target_date = วันเสาร์` (ถ้าตลาดปิดแล้ว)

2. **วันจันทร์เย็นรัน:**
   - ระบบจะทำนาย `target_date = วันอังคาร` (ถูกต้อง)
   - `verify_forecast()` จะ verify forecast ที่ `target_date = วันจันทร์` (ถ้าตลาดปิดแล้ว)

---

## 🎯 **สรุป: ควรรันเมื่อไหร่?**

### ✅ **แนะนำ: รันทุกวันตอนเย็น (หลังตลาดปิด)**

**ตารางเวลา:**
- **วันจันทร์-พฤหัสบดี:** รันตอนเย็น (หลังตลาดปิด) → ทำนายวันถัดไป
- **วันศุกร์:** รันตอนเย็น (หลังตลาดปิด) → ทำนายวันจันทร์ (วันทำการถัดไป)
- **วันเสาร์-อาทิตย์:** ไม่ต้องรัน (ตลาดปิด)

**คำสั่ง:**
```bash
python main.py
```

---

## ⚠️ **ข้อจำกัดปัจจุบัน:**

**ระบบยังไม่ได้เช็คว่า `target_date` เป็นวันทำการหรือไม่:**
- ถ้าวันศุกร์รัน → `target_date = วันเสาร์` (แต่ตลาดปิด)
- ถ้าวันจันทร์รัน → `target_date = วันอังคาร` (ถูกต้อง)

**แต่ `verify_forecast()` จะ verify ได้เพราะ:**
- `target_date <= today` → วันเสาร์ <= วันจันทร์ ✅
- `is_market_closed()` → วันเสาร์ = ปิด ✅
- → จะ verify ได้ (แต่จะใช้ราคาวันจันทร์มาเปรียบเทียบ)

---

## 🔧 **แนะนำ: ปรับปรุงในอนาคต**

**ควรเพิ่มฟังก์ชัน `get_next_trading_day()` เพื่อ:**
- ถ้าวันศุกร์รัน → `target_date = วันจันทร์` (วันทำการถัดไป)
- ถ้าวันจันทร์รัน → `target_date = วันอังคาร` (วันทำการถัดไป)

**ตัวอย่างโค้ด:**
```python
def get_next_trading_day(date, exchange):
    """หาวันทำการถัดไป (ข้ามวันหยุด)"""
    from datetime import timedelta
    next_day = date + timedelta(days=1)
    
    # ข้ามวันเสาร์-อาทิตย์
    while next_day.weekday() >= 5:  # 5=Saturday, 6=Sunday
        next_day += timedelta(days=1)
    
    # ข้ามวันหยุด (ถ้ามี)
    # TODO: เพิ่ม logic เช็ควันหยุดของแต่ละตลาด
    
    return next_day
```

---

## 📋 **สรุป:**

### ✅ **คำตอบ: ใช่ ควรรันวันนี้ตอนเย็น (วันศุกร์)**

**เหตุผล:**
1. ระบบจะทำนาย `target_date = วันเสาร์` (แม้ตลาดปิด)
2. `verify_forecast()` จะ verify เมื่อ `target_date <= today` และตลาดปิด
3. ถ้าวันจันทร์เย็นรัน → จะ verify forecast ที่ `target_date = วันเสาร์` (ถ้าตลาดปิดแล้ว)

**แต่ควรปรับปรุงในอนาคต:**
- เพิ่มฟังก์ชัน `get_next_trading_day()` เพื่อให้ `target_date` เป็นวันทำการถัดไปเสมอ

