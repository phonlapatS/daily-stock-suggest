# คู่มือ: วิธีคำนวณค่าต่างๆในตาราง Scanner

## 📊 ตัวอย่างตาราง Scanner

```
Symbol   Price Change Streak_Status Thres. WinRate AvgRet MaxRisk  Events
PTT     ฿32.75 +3.15%  🟢 Up 1 Days  1.61%   43.0% +0.13%  -9.43%     272
```

มาทำความเข้าใจทีละคอลัมน์กันครับ!

---

## 1️⃣ **Price** (ราคา)

**ความหมาย:** ราคาปิดล่าสุดของหุ้น

**วิธีคำนวณ:**
```python
price = df['close'].iloc[-1]  # ราคาล่าสุด
```

**ตัวอย่าง:**
```
วันนี้: PTT ปิดที่ 32.75 บาท
→ Price = ฿32.75
```

---

## 2️⃣ **Change** (% เปลี่ยนแปลง)

**ความหมาย:** % การเปลี่ยนแปลงของราคาจากวันก่อนหน้า

**วิธีคำนวณ:**
```python
change = ((close_today - close_yesterday) / close_yesterday) * 100
```

**ตัวอย่าง:**
```
เมื่อวาน: PTT = 31.75
วันนี้: PTT = 32.75

Change = ((32.75 - 31.75) / 31.75) × 100
       = (1.00 / 31.75) × 100
       = 3.15%
```

---

## 3️⃣ **Threshold** (เกณฑ์ความสำคัญ)

**ความหมาย:** เปอร์เซ็นต์ที่ถือว่าหุ้นเคลื่อนไหว "มีนัยสำคัญ"

### **วิธีที่ 1: SD-based (Scanner V1)**
```python
# ใช้ Standard Deviation × 1.5
std = df['pct_change'].tail(90).std()
threshold = std × 1.5
threshold = max(0.5, min(threshold, 5.0))  # จำกัด 0.5-5%
```

**ตัวอย่าง:**
```
PTT ใน 90 วันที่ผ่านมา:
วันที่ 1: +2.0%
วันที่ 2: -1.5%
วันที่ 3: +0.8%
... (อีก 87 วัน)

SD = 1.07%
Threshold = 1.07 × 1.5 = 1.61%

ความหมาย: ถ้า PTT เปลี่ยนแปลง > 1.61% ถือว่า "significant move"
```

### **วิธีที่ 2: Percentile (Scanner V2)**
```python
# ใช้ 90th percentile ของ 126 วัน
threshold = df['pct_change'].abs().rolling(126).quantile(0.90)
threshold = max(threshold, 1.0)  # ขั้นต่ำ 1%
```

**ตัวอย่าง:**
```
PTT ใน 126 วัน:
เรียงค่า absolute change:
0.1%, 0.2%, ..., 1.5%, 1.6%, 2.0%, 2.5%, 3.0%
                              ↑
                         90th percentile

Threshold = 2.5%
```

---

## 4️⃣ **Streak** (ลำดับวันติดต่อกัน)

**ความหมาย:** จำนวนวันที่หุ้นเคลื่อนไหว "ทิศทางเดียวกัน" เกิน threshold

### **V1: Directional Streak**
```python
streak = 0
current_direction = None

# ไล่ย้อนจากวันล่าสุด
for i in range(len(df)-1, -1, -1):
    change = df.iloc[i]['pct_change']
    
    if change > threshold:
        direction = 'UP'
    elif change < -threshold:
        direction = 'DOWN'
    else:
        break  # ไม่เกิน threshold = หยุดนับ
    
    if current_direction == direction:
        streak += 1
    else:
        break  # ทิศทางเปลี่ยน = หยุด
```

**ตัวอย่าง:**
```
Threshold = 1.61%

วันที่ 1: +3.15% > 1.61% → Up ✅
วันที่ 2: +2.50% > 1.61% → Up ✅  
วันที่ 3: +2.10% > 1.61% → Up ✅
วันที่ 4: +0.80% < 1.61% → Break! ❌

Streak = +3 (Up 3 Days)
```

### **V2: Mixed Streak** (ไม่สนทิศทาง)
```python
# นับทุกวันที่ abs(change) > threshold
# ไม่สนว่า + หรือ -

วันที่ 1: +3.15% → significant ✅
วันที่ 2: -2.50% → significant ✅ (mixed!)
วันที่ 3: +2.10% → significant ✅
วันที่ 4: +0.80% → not significant ❌

Streak = 3 (Volatility streak)
```

---

## 5️⃣ **Events** ⭐ (จุดสำคัญ!)

**ความหมาย:** จำนวนครั้งที่เคยเกิด pattern เดียวกันในอดีต

### **วิธีคำนวณ:**

```python
# 1. ดู Streak ปัจจุบัน
current_streak = 3  # Up 3 Days

# 2. หาในอดีตว่าเคยมี Up 3 Days กี่ครั้ง
events = 0

for i in range(len(df) - 1):
    # คำนวณ streak ณ วันที่ i
    if streak_at_day_i == 3:  # เจอ!
        events += 1
```

**ตัวอย่างละเอียด:**

```
PTT มีข้อมูล 3000 วัน
Streak ปัจจุบัน: Up 3 Days (วันนี้)

หาย้อนหลังว่า เคยมี "Up 3 Days" กี่ครั้ง:

วันที่ 100-102: Up, Up, Up → เจอ! event #1
วันที่ 250-252: Up, Up, Up → เจอ! event #2
วันที่ 500-502: Up, Up, Up → เจอ! event #3
...
วันที่ 2990-2992: Up, Up, Up → เจอ! event #272

Events = 272 ครั้ง
```

**ทำไมต้องรู้ Events?**
- Events สูง = มีข้อมูลเยอะ = น่าเชื่อถือมากขึ้น
- Events ต่ำ = ข้อมูลน้อย = ไม่แน่ใจ

---

## 6️⃣ **WinRate** (อัตราชนะ)

**ความหมาย:** เมื่อเคยเกิด pattern นี้ในอดีต พรุ่งนี้มักเป็นบวก % เท่าไหร่

### **วิธีคำนวณ:**

```python
# จาก Events ที่หาได้ (272 ครั้ง)
# ดูว่าวันถัดไปเป็นบวกกี่ครั้ง

wins = 0
total = 0

for event in all_events:
    next_day_return = get_next_day_return(event)
    
    if next_day_return > 0:
        wins += 1
    
    total += 1

win_rate = (wins / total) × 100
```

**ตัวอย่าง:**

```
Events = 272 ครั้งที่เคยมี "Up 3 Days"

ดูวันถัดไป:
Event #1: Up 3 Days → Next day: +0.5% ✅
Event #2: Up 3 Days → Next day: -0.3% ❌
Event #3: Up 3 Days → Next day: +0.8% ✅
Event #4: Up 3 Days → Next day: +0.2% ✅
...
Event #272: Up 3 Days → Next day: -0.1% ❌

Wins: 117 ครั้ง (เป็นบวก)
Total: 272 ครั้ง

WinRate = (117 / 272) × 100 = 43.0%
```

**การตีความ:**
- WinRate = 50% → Random (เหมือนโยนเหรียญ)
- WinRate > 60% → มีแนวโน้มขึ้น
- WinRate < 40% → มีแนวโน้มลง

---

## 7️⃣ **AvgRet** (Return เฉลี่ย)

**ความหมาย:** เมื่อเกิด pattern นี้ วันถัดไปเปลี่ยนแปลงเฉลี่ยเท่าไหร่

### **วิธีคำนวณ:**

```python
total_return = 0

for event in all_events:
    next_day_return = get_next_day_return(event)
    total_return += next_day_return

avg_return = total_return / len(all_events)
```

**ตัวอย่าง:**

```
Events = 272 ครั้ง

Next day returns:
+0.5%, -0.3%, +0.8%, +0.2%, -0.1%, +0.6%, ...

ผลรวม: +35.36%
AvgRet = 35.36 / 272 = +0.13%
```

**การตีความ:**
- AvgRet = +0.13% → โดยเฉลี่ยพรุ่งนี้มักขึ้น 0.13%
- AvgRet ใกล้ 0 → ไม่มี edge
- AvgRet สูง → มี edge ดี

---

## 8️⃣ **MaxRisk** (ความเสี่ยงสูงสุด)

**ความหมาย:** ผลลัพธ์แย่ที่สุดที่เคยเกิดขึ้น

### **วิธีคำนวณ:**

```python
max_risk = min(all_next_day_returns)  # หาค่าต่ำสุด
```

**ตัวอย่าง:**

```
Events = 272 ครั้ง

Next day returns:
+0.5%, -0.3%, +0.8%, +0.2%, -9.43%, +0.6%, ...
                                ↑
                           worst case!

MaxRisk = -9.43%
```

**การตีความ:**
- MaxRisk = -9.43% → ในกรณีแย่สุด ขาดทุน 9.43%
- ต้องพร้อมรับความเสี่ยงนี้ได้

---

## 🎯 สรุปตัวอย่างทั้งหมด

```
Symbol: PTT
Price: ฿32.75
Change: +3.15%
Streak: Up 3 Days
Threshold: 1.61%

คำนวณ Historical Probability:
1. หา Events = 272 ครั้งที่เคยมี "Up 3 Days"
2. ดูวันถัดไป:
   - Wins: 117 ครั้ง (บวก)
   - Losses: 155 ครั้ง (ลบ)
3. WinRate = 117/272 = 43.0%
4. AvgRet = sum(all returns) / 272 = +0.13%
5. MaxRisk = min(all returns) = -9.43%
```

---

## 💡 ตัวอย่างการอ่านผล

```
PTT: WinRate 43.0%, AvgRet +0.13%, MaxRisk -9.43%, Events 272
```

**แปลว่า:**
- เคยเกิด pattern นี้ 272 ครั้ง (ข้อมูลเยอะ ✅)
- พรุ่งนี้มีโอกาส 43% ที่จะขึ้น (ต่ำกว่า 50% ⚠️)
- โดยเฉลี่ยขึ้น 0.13% (น้อยมาก)
- แต่เคยขาดทุนสูงสุด 9.43% (Risk สูง! ❌)

**สรุป:** Signal ไม่แข็งแรง ความเสี่ยงสูง → ระวัง!

---

## ✅ Checklist การดูตาราง

เวลาดูตาราง ให้เช็คตามนี้:

1. **Events >= 30?** → ข้อมูลเพียงพอ
2. **WinRate > 55%?** → มี edge
3. **AvgRet > 0.3%?** → คุ้มค่า
4. **MaxRisk สามารถรับได้?** → Position sizing

**ตัวอย่างหุ้นดี:**
```
WinRate 65%, AvgRet +0.8%, MaxRisk -3%, Events 150
→ ดี! มี edge ชัดเจน
```

**ตัวอย่างหุ้นไม่ดี:**
```
WinRate 48%, AvgRet +0.05%, MaxRisk -15%, Events 8
→ ไม่ดี! ข้อมูลน้อย, ไม่มี edge, Risk สูง
```

---

**หวังว่าทำความเข้าใจแล้วนะครับ! 📊✨**
