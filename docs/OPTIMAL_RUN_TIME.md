# ⏰ เวลาที่เหมาะสมสำหรับรันทีเดียวทุกตลาด

## 📊 วิเคราะห์เวลาปิดตลาด (ICT)

| ตลาด | เวลาปิด (ICT) | สถานะตอน 17:00 | สถานะตอน 04:00 |
|------|---------------|-----------------|----------------|
| **TWSE** (Taiwan) | 12:30 | ✅ ปิดแล้ว | ⚠️ ปิดไปแล้ว 1 วัน |
| **China** (SHSE/SZSE) | 14:00 | ✅ ปิดแล้ว | ⚠️ ปิดไปแล้ว 1 วัน |
| **HKEX** (Hong Kong) | 15:00 | ✅ ปิดแล้ว | ⚠️ ปิดไปแล้ว 1 วัน |
| **SET/MAI** (Thailand) | 16:30 | ✅ ปิดแล้ว | ⚠️ ปิดไปแล้ว 1 วัน |
| **NASDAQ/NYSE** (US) | 03:00-04:00 (วันถัดไป) | ❌ ยังไม่ปิด | ✅ ปิดแล้ว |

---

## ❌ **ไม่มีเวลาที่รันทีเดียวได้ทุกตลาดพร้อมกัน**

**เหตุผล:**
- US market ปิดช้ากว่ามาก (03:00-04:00 ICT วันถัดไป)
- ถ้ารอให้ US ปิด → ตลาดเอเชียปิดไปแล้ว 1 วัน (ข้อมูลไม่ fresh)

---

## ✅ **วิธีที่ดีที่สุด: รัน 2 ครั้ง**

### 🕐 **17:00 ICT** - ตลาดเอเชีย (แนะนำ)

**ครอบคลุม:**
- ✅ SET/MAI (Thailand)
- ✅ HKEX (Hong Kong)
- ✅ SHSE/SZSE (China)
- ✅ TWSE (Taiwan)
- ❌ NASDAQ/NYSE (จะถูก skip อัตโนมัติ)

**คำสั่ง:**
```bash
python main.py
```

**ผลลัพธ์:**
- ระบบจะทำนาย SET, HKEX, China, TWSE
- US จะถูก skip (เพราะยังไม่ปิด) → ไม่เสียเวลา

---

### 🕐 **04:00 ICT** (เช้าวันถัดไป) - US Market

**ครอบคลุม:**
- ✅ NASDAQ/NYSE (US)

**คำสั่ง:**
```bash
python main.py --group GROUP_B_US
```

**ผลลัพธ์:**
- ทำนาย US market สำหรับวันถัดไป

---

## 📈 **สรุปเปรียบเทียบ**

| เวลา | ครอบคลุม | ข้อมูล Fresh | จำนวนครั้ง |
|------|----------|--------------|------------|
| **17:00 ICT** | เอเชียทั้งหมด | ✅ Fresh | 1 ครั้ง |
| **04:00 ICT** | US เท่านั้น | ✅ Fresh | 1 ครั้ง |
| **รวม** | **ทุกตลาด** | ✅ Fresh | **2 ครั้ง** |

---

## 💡 **คำแนะนำ**

### วิธีที่ 1: รัน 2 ครั้ง (แนะนำ) ⭐

```bash
# 17:00 ICT - ตลาดเอเชีย
python main.py

# 04:00 ICT (เช้าวันถัดไป) - US
python main.py --group GROUP_B_US
```

**ข้อดี:**
- ✅ ข้อมูล fresh ทุกตลาด
- ✅ ไม่เสียเวลาไปกับตลาดที่ยังไม่ปิด
- ✅ ครอบคลุมทุกตลาด

---

### วิธีที่ 2: รันครั้งเดียวตอน 17:00 (ถ้าขี้เกียจ)

```bash
# 17:00 ICT - รันทุกตลาด
python main.py
```

**ผลลัพธ์:**
- ✅ SET, HKEX, China, TWSE → ทำนายได้
- ⚠️ US → ถูก skip (ต้องรันแยกตอนเช้า)

**ข้อดี:**
- ✅ รันครั้งเดียว → สะดวก
- ⚠️ US ต้องรันแยก

---

## 🔄 **Auto-Scheduler (แนะนำ)**

สร้าง script สำหรับรันอัตโนมัติ:

```python
# scripts/auto_scheduler.py
import schedule
import time
import os
from datetime import datetime

def run_asia_markets():
    """รันตลาดเอเชียหลังปิด (17:00 ICT)"""
    print(f"[{datetime.now()}] Running Asia markets (SET, HKEX, China, TWSE)...")
    os.system("python main.py")

def run_us_market():
    """รัน US market หลังปิด (04:00 ICT)"""
    print(f"[{datetime.now()}] Running US market...")
    os.system("python main.py --group GROUP_B_US")

# Schedule
schedule.every().day.at("17:00").do(run_asia_markets)  # เอเชีย
schedule.every().day.at("04:00").do(run_us_market)    # US

print("🕐 Auto-scheduler started. Waiting for scheduled times...")
while True:
    schedule.run_pending()
    time.sleep(60)
```

---

## 📝 **สรุป**

**ไม่มีเวลากลางๆ ที่รันทีเดียวได้ทุกตลาด** เพราะ:
- US market ปิดช้ากว่ามาก (03:00-04:00 ICT วันถัดไป)
- ถ้ารอให้ US ปิด → ตลาดเอเชียปิดไปแล้ว 1 วัน

**วิธีที่ดีที่สุด:**
- **17:00 ICT** → รันตลาดเอเชีย (SET, HKEX, China, TWSE)
- **04:00 ICT** → รัน US market

**รวม 2 ครั้ง/วัน = ครอบคลุมทุกตลาด + ข้อมูล fresh!** ✅

