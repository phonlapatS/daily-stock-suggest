# 📊 Basic System Optimization

**วันที่:** 2026-02-13  
**เป้าหมาย:** เพิ่มจำนวนหุ้นที่เล่นได้ โดยยังคงคุณภาพดี

---

## 🔧 การปรับปรุง

### **1. Prob Threshold: 60% → 55%**
- **เดิม:** Prob >= 60%
- **ใหม่:** Prob >= 55%
- **เหตุผล:** หุ้นส่วนใหญ่ Prob อยู่ที่ 50-58% → ลด threshold เพื่อให้ได้สัญญาณมากขึ้น

### **2. Min Stats: 30 → 25**
- **เดิม:** match_count >= 30
- **ใหม่:** match_count >= 25
- **เหตุผล:** ลดความเข้มงวด → ได้ pattern มากขึ้น

### **3. RRR Filter: None → 1.2**
- **เดิม:** ไม่มี RRR filter
- **ใหม่:** RRR >= 1.2 (secondary filter)
- **เหตุผล:** ใช้ RRR เป็น secondary filter เพื่อกรองหุ้นที่ RRR ต่ำเกินไป

---

## 📊 เกณฑ์ใหม่

### **Primary Criteria:**
1. ✅ **Prob >= 55%** (ลดจาก 60%)
2. ✅ **Match Count >= 25** (ลดจาก 30)

### **Secondary Criteria:**
3. ✅ **RRR >= 1.2** (เพิ่มใหม่)

---

## 🎯 ผลลัพธ์ที่คาดหวัง

### **จากผลลัพธ์เดิม:**
- ✅ Passed: 2 หุ้น (0.9%)
- ❌ No-Trade: 145 หุ้น (63.3%)
- ⏭️ No Pattern Found: 82 หุ้น (35.8%)

### **ผลลัพธ์ที่คาดหวัง:**
- ✅ **Passed: ~20-40 หุ้น** (8-17%)
  - หุ้นที่ Prob 55-60% จะผ่านเกณฑ์
  - หุ้นที่ Prob >= 55% + RRR >= 1.2

### **ตัวอย่างหุ้นที่ควรผ่าน:**
- EPG: Prob 58.4%, RRR 1.74, Matches 89 → ✅ (Prob > 55%, RRR > 1.2)
- META: Prob 58.3%, RRR 1.12, Matches 834 → ✅ (Prob > 55%, RRR > 1.2)
- KDP: Prob 57.2%, RRR 0.94, Matches 1447 → ❌ (RRR < 1.2)
- FISV: Prob 57.7%, RRR 1.25, Matches 279 → ✅ (Prob > 55%, RRR > 1.2)
- ICHI: Prob 55.7%, RRR 1.34, Matches 318 → ✅ (Prob > 55%, RRR > 1.2)

---

## 📈 เปรียบเทียบ

| Aspect | เดิม | ใหม่ |
|--------|-----|------|
| **Prob Threshold** | 60% | 55% |
| **Min Stats** | 30 | 25 |
| **RRR Filter** | ไม่มี | >= 1.2 |
| **Passed Rate (คาดหวัง)** | 0.9% (2/229) | 8-17% (20-40/229) |
| **คุณภาพ** | สูงมาก (Prob 67%, RRR 3.71) | ดี (Prob 55-60%, RRR >= 1.2) |

---

## ✅ ข้อดี

1. **ได้สัญญาณมากขึ้น:** จาก 2 → ~20-40 หุ้น
2. **ยังคงคุณภาพ:** Prob >= 55%, RRR >= 1.2
3. **สมดุล:** ระหว่างคุณภาพและจำนวน

---

## ⚠️ ข้อควรระวัง

1. **Prob ต่ำกว่า:** จาก 67% → ~55-60%
2. **RRR ต่ำกว่า:** จาก 3.71 → ~1.2-2.0
3. **ต้องทดสอบ:** ควรรัน backtest เพื่อดูผลลัพธ์จริง

---

## 🚀 Next Steps

1. **รัน Backtest:**
   ```bash
   python scripts/backtest_basic.py --full --bars 2500
   ```

2. **วิเคราะห์ผลลัพธ์:**
   - จำนวนหุ้นที่ผ่าน
   - Prob% เฉลี่ย
   - RRR เฉลี่ย
   - Match Count เฉลี่ย

3. **ปรับปรุงเพิ่มเติม (ถ้าจำเป็น):**
   - ลด Prob เป็น 53% (ถ้ายังได้น้อย)
   - เพิ่ม RRR เป็น 1.5 (ถ้าต้องการคุณภาพสูงขึ้น)
   - ปรับ min_stats ตามประเทศ

---

## 🔗 Related Documents

- [BASIC_SYSTEM_RESULTS_ANALYSIS.md](BASIC_SYSTEM_RESULTS_ANALYSIS.md) - ผลลัพธ์เดิม
- [BACK_TO_BASIC_ANALYSIS.md](BACK_TO_BASIC_ANALYSIS.md) - แนวทาง Back to Basic

