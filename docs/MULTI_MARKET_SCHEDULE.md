# 📅 ตารางเวลารันระบบสำหรับหลายตลาด

## 🕐 เวลาปิดตลาด (ICT - Thailand Time)

| ตลาด | เวลาปิด (Local) | เวลาปิด (ICT) | เวลารันแนะนำ |
|------|------------------|---------------|--------------|
| **TWSE** (Taiwan) | 13:30 CST | **12:30 ICT** | 13:00 ICT |
| **SHSE/SZSE** (China) | 15:00 CST | **14:00 ICT** | 14:30 ICT |
| **HKEX** (Hong Kong) | 16:00 HKT | **15:00 ICT** | 15:30 ICT |
| **SET/MAI** (Thailand) | 16:30 ICT | **16:30 ICT** | 17:00 ICT |
| **NASDAQ/NYSE** (US) | 16:00 EST | **03:00-04:00 ICT** (วันถัดไป) | 04:00 ICT |

---

## 🎯 วิธีรันที่คุ้มค่า (3 วิธี)

### วิธีที่ 1: รันแยกตามเวลาตลาดปิด (แนะนำ) ⭐

**ข้อดี:**
- ✅ รันเฉพาะตลาดที่ปิดแล้ว → ข้อมูล fresh
- ✅ ไม่เสียเวลาไปกับตลาดที่ยังไม่ปิด
- ✅ ใช้ทรัพยากรน้อย

**ตารางเวลา:**
```bash
# 13:00 ICT - Taiwan
python main.py --group GROUP_D_TAIWAN

# 14:30 ICT - China
python main.py --group GROUP_C_CHINA

# 15:30 ICT - Hong Kong
python main.py --group GROUP_E_HK

# 17:00 ICT - Thailand
python main.py --group GROUP_A_THAI

# 04:00 ICT (เช้าวันถัดไป) - US
python main.py --group GROUP_B_US
```

---

### วิธีที่ 2: รันครั้งเดียวตอนเย็น (17:00 ICT) 

**ข้อดี:**
- ✅ รันครั้งเดียว → สะดวก
- ✅ ระบบจะ skip ตลาดที่ยังไม่ปิดอัตโนมัติ (มี `should_skip_symbol()`)

**ข้อเสีย:**
- ⚠️ US market ยังไม่ปิด → จะ skip
- ⚠️ ต้องรัน US แยกตอนเช้า (04:00 ICT)

**คำสั่ง:**
```bash
# 17:00 ICT - รันทุกตลาด (ยกเว้น US)
python main.py

# 04:00 ICT (เช้าวันถัดไป) - รัน US
python main.py --group GROUP_B_US
```

**Logic:**
- ระบบจะเช็ค `is_market_closed()` ก่อนทำนาย
- ถ้าตลาดยังไม่ปิด → skip อัตโนมัติ
- ไม่เสียเวลาไปกับตลาดที่ยังไม่ปิด

---

### วิธีที่ 3: รันเฉพาะตลาดที่สนใจ

**เหมาะสำหรับ:**
- เน้นเฉพาะตลาดที่เทรดจริง
- ลดเวลาและทรัพยากร

**ตัวอย่าง:**
```bash
# รันเฉพาะ SET + US
python main.py --group GROUP_A_THAI
python main.py --group GROUP_B_US
```

---

## 🔄 Auto-Scheduler (แนะนำให้เพิ่ม)

สร้าง script สำหรับรันอัตโนมัติตามเวลาตลาดปิด:

```python
# scripts/auto_scheduler.py
import schedule
import time
from datetime import datetime

def run_thai_market():
    """รัน SET/MAI หลังตลาดปิด"""
    print(f"[{datetime.now()}] Running Thai market scan...")
    os.system("python main.py --group GROUP_A_THAI")

def run_us_market():
    """รัน US หลังตลาดปิด (เช้าวันถัดไป)"""
    print(f"[{datetime.now()}] Running US market scan...")
    os.system("python main.py --group GROUP_B_US")

def run_taiwan_market():
    """รัน Taiwan หลังตลาดปิด"""
    print(f"[{datetime.now()}] Running Taiwan market scan...")
    os.system("python main.py --group GROUP_D_TAIWAN")

# Schedule
schedule.every().day.at("13:00").do(run_taiwan_market)  # TWSE
schedule.every().day.at("14:30").do(run_china_market)   # China
schedule.every().day.at("15:30").do(run_hk_market)      # HKEX
schedule.every().day.at("17:00").do(run_thai_market)    # SET
schedule.every().day.at("04:00").do(run_us_market)      # US

while True:
    schedule.run_pending()
    time.sleep(60)
```

---

## 📊 สรุปเปรียบเทียบ

| วิธี | จำนวนครั้ง/วัน | เวลารวม | ข้อดี | ข้อเสีย |
|------|----------------|---------|-------|---------|
| **แยกตามเวลา** | 5 ครั้ง | ~5-10 นาที/ครั้ง | Fresh data, ไม่เสียเวลา | ต้องรันหลายครั้ง |
| **รันครั้งเดียว** | 2 ครั้ง | ~30-60 นาที/ครั้ง | สะดวก | US ต้องรันแยก |
| **เฉพาะตลาด** | 1-2 ครั้ง | ~10-20 นาที/ครั้ง | เร็ว, ใช้ทรัพยากรน้อย | ไม่ครอบคลุมทุกตลาด |

---

## 💡 คำแนะนำ

1. **ถ้าเทรดหลายตลาด** → ใช้วิธีที่ 1 (แยกตามเวลา) + Auto-Scheduler
2. **ถ้าเทรดเฉพาะ SET/US** → ใช้วิธีที่ 3 (เฉพาะตลาด)
3. **ถ้าต้องการความสะดวก** → ใช้วิธีที่ 2 (รันครั้งเดียว) แต่ต้องรัน US แยก

---

## 🔍 ตรวจสอบว่าตลาดปิดหรือยัง

```python
from core.market_time import is_market_closed

is_closed, status, close_time = is_market_closed('SET')
print(f"SET: {status}")  # "Market closed (closes at 16:30 ICT)" หรือ "Market still open..."
```

