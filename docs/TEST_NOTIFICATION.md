# Test Notification Guide

## 📢 การแจ้งเตือนเมื่อทดสอบเสร็จ

### สถานะปัจจุบัน:
- ⏳ **Test is running in background**
- 📊 **18 combinations** กำลังทดสอบ
- ⏱️ **Expected time:** 30-60 minutes

---

## 🔍 วิธีตรวจสอบสถานะ

### Option 1: ใช้สคริปต์ตรวจสอบ
```bash
python scripts/check_test_status.py
```

### Option 2: ใช้สคริปต์รอและแจ้ง
```bash
python scripts/notify_when_done.py
```
(จะรอจนกว่าจะเสร็จแล้วแสดงผลลัพธ์)

### Option 3: ตรวจสอบไฟล์โดยตรง
```bash
# ตรวจสอบว่ามีไฟล์ผลลัพธ์หรือไม่
ls data/china_realistic_settings_results.csv

# ถ้ามีแล้ว อ่านผลลัพธ์
python -c "import pandas as pd; df = pd.read_csv('data/china_realistic_settings_results.csv'); print(df.sort_values('score', ascending=False).head(5).to_string())"
```

---

## 📊 เมื่อทดสอบเสร็จ

### จะได้:
1. ✅ **Comparison Table** - ตารางเปรียบเทียบ 18 combinations
2. ✅ **Best Combination** - ค่าที่ดีที่สุด
3. ✅ **Assessment** - Acceptable หรือ Not
4. ✅ **Recommendations** - คำแนะนำ

### ถ้า Acceptable:
- ✅ แสดงผลลัพธ์
- ✅ แนะนำให้ใช้ค่าที่ดีที่สุด
- ✅ พร้อมปรับ `backtest.py`

### ถ้า Not Acceptable:
- ⚠️ แสดงปัญหา
- 💡 แนะนำทางเลือกอื่น

---

## 🎯 ขณะนี้

**Status:** ⏳ **Test is running in background**

**Monitoring:** `scripts/notify_when_done.py` กำลังรอและจะแจ้งเมื่อเสร็จ

**Expected Completion:** 30-60 minutes from start

---

**Last Updated:** 2026-02-13  
**Status:** ⏳ **WAITING FOR COMPLETION**

