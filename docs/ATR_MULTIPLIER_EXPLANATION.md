# ATR Multiplier คืออะไร?

## 📌 คำตอบสั้นๆ

**ATR Multiplier** คือตัวคูณกับ **ATR (Average True Range)** เพื่อกำหนดระยะห่างของ **Stop Loss** จากราคา

**ไม่ใช่:**
- ❌ SD (Standard Deviation) ที่ใช้ใน Pattern Detection
- ❌ Threshold ที่ใช้ในการหา Pattern

**คือ:**
- ✅ ตัวคูณกับ ATR เพื่อกำหนดระยะห่างของ Trailing Stop Loss

---

## 🔍 ATR คืออะไร?

**ATR (Average True Range)** = ตัววัดความผันผวน (Volatility) ของราคา

### สูตร ATR:
```
True Range (TR) = max(
    High - Low,
    |High - Previous Close|,
    |Low - Previous Close|
)

ATR = Moving Average ของ TR (14 periods)
```

### ตัวอย่าง:
```
หุ้น A:
- ATR = 2 บาท (ราคาผันผวนเฉลี่ย 2 บาทต่อวัน)

หุ้น B:
- ATR = 0.5 บาท (ราคาผันผวนเฉลี่ย 0.5 บาทต่อวัน)
```

---

## 🎯 ATR Multiplier คืออะไร?

**ATR Multiplier** = ตัวกำหนดว่า Stop Loss จะอยู่ห่างจากราคาเท่าไหร่ (เป็นเท่าไหร่ของ ATR)

### สูตร Trailing Stop:
```
Trailing Stop = Highest Price - (ATR × Multiplier)
```

### ตัวอย่าง:

**หุ้น A: ATR = 2 บาท, Entry = 100 บาท**

| Multiplier | Stop Loss Distance | Trailing Stop (ถ้า High = 105) |
|------------|-------------------|--------------------------------|
| 1.0        | 2 บาท            | 105 - (2 × 1.0) = 103 บาท      |
| 1.5        | 3 บาท            | 105 - (2 × 1.5) = 102 บาท      |
| 2.0        | 4 บาท            | 105 - (2 × 2.0) = 101 บาท      |
| 2.5        | 5 บาท            | 105 - (2 × 2.5) = 100 บาท      |

---

## 📊 Multiplier กับพฤติกรรมตลาด

### 1. **Trend Following (US Market)**
- **Multiplier = 2.5** (หลวม)
- เหตุผล: ให้กำไรเดินทาง (let profit run)
- ตัวอย่าง: ถ้า ATR = 2 บาท → Stop ห่าง 5 บาท

### 2. **Mean Reversion (Thai/China Market)**
- **Multiplier = 1.5** (แน่น)
- เหตุผล: Lock profit เร็ว (เพราะ Mean Reversion จะกลับตัวเร็ว)
- ตัวอย่าง: ถ้า ATR = 2 บาท → Stop ห่าง 3 บาท

---

## 🔄 เปรียบเทียบกับ SD และ Threshold

### SD (Standard Deviation)
- **ใช้ที่ไหน**: Pattern Detection (หา Threshold)
- **สูตร**: `Threshold = SD × Threshold Multiplier`
- **ตัวอย่าง**: `Threshold = 0.02 × 1.25 = 0.025 (2.5%)`
- **จุดประสงค์**: กำหนดว่า price move แค่ไหนถึงเรียกว่า "significant"

### Threshold
- **ใช้ที่ไหน**: Pattern Detection (หา +/- pattern)
- **สูตร**: `Threshold = max(SD_20d, SD_252d, Min_Floor) × Threshold Multiplier`
- **ตัวอย่าง**: `Threshold = 0.01 × 1.25 = 0.0125 (1.25%)`
- **จุดประสงค์**: กำหนดว่า price move แค่ไหนถึงเป็น pattern

### ATR Multiplier
- **ใช้ที่ไหน**: Trailing Stop Loss (กำหนดระยะห่าง Stop)
- **สูตร**: `Trailing Stop = Highest Price - (ATR × ATR Multiplier)`
- **ตัวอย่าง**: `Trailing Stop = 105 - (2 × 2.0) = 101 บาท`
- **จุดประสงค์**: กำหนดว่า Stop Loss จะอยู่ห่างจากราคาเท่าไหร่

---

## 📝 สรุป

| ตัวแปร | ใช้ที่ไหน | สูตร | ตัวอย่าง |
|--------|----------|------|----------|
| **SD** | Pattern Detection | `Threshold = SD × 1.25` | `0.02 × 1.25 = 0.025` |
| **Threshold** | Pattern Detection | `Threshold = max(SD_20d, SD_252d) × 1.25` | `0.01 × 1.25 = 0.0125` |
| **ATR Multiplier** | Trailing Stop Loss | `Stop = Price - (ATR × Multiplier)` | `105 - (2 × 2.0) = 101` |

---

## 🎯 ตัวอย่างจริง

### หุ้น PTTEP (Thai Market)
```
Entry Price: 100 บาท
ATR: 2 บาท
Multiplier: 1.5 (Mean Reversion)

Initial Stop = 100 - (2 × 1.5) = 97 บาท
```

### ถ้าราคาเดินทางไปที่ 105 บาท:
```
Highest Price: 105 บาท
Trailing Stop = 105 - (2 × 1.5) = 102 บาท
```

### ถ้าราคา pullback มาที่ 101 บาท:
```
Low = 101 บาท <= Trailing Stop (102 บาท)
→ Exit ที่ 102 บาท
→ Profit = (102 - 100) / 100 = +2%
```

---

## ✅ สรุป

**ATR Multiplier** คือ:
- ✅ ตัวคูณกับ ATR เพื่อกำหนดระยะห่างของ Stop Loss
- ✅ ไม่ใช่ SD หรือ Threshold ที่ใช้ใน Pattern Detection
- ✅ ปรับตามพฤติกรรมตลาด (Trend Following vs Mean Reversion)
- ✅ เป้าหมาย: ให้ RRR > 2.0 โดยการ lock profit และป้องกัน loss

