# Logic Engine ที่ใช้ Prob%, AvgWin%, AvgLoss%, RRR (ตาม Mentor)

## 📊 สรุป

**ตามที่ Mentor ต้องการ:** ใช้ Prob%, AvgWin%, AvgLoss%, และ RRR เท่านั้น

### เกณฑ์เดิม (เข้มงวดเกินไป)
- **Prob > 60% AND RRR > 2.0**
- **ผลลัพธ์:** 3 หุ้น

### เกณฑ์ใหม่ (ใช้ Prob%, AvgWin%, AvgLoss%, RRR)
- **หลายเกณฑ์** ที่ใช้ Prob%, AvgWin%, AvgLoss%, RRR
- **ผลลัพธ์:** 39 หุ้น

## 🎯 เกณฑ์ต่างๆ ที่ใช้ Prob%, AvgWin%, AvgLoss%, RRR

### 1. STRICT (เกณฑ์เข้มงวด)
```
Prob > 60% AND RRR > 2.0 AND Count >= 10
```
- **ผลลัพธ์:** 3 หุ้น
- **ตัวอย่าง:** PTTEP (Prob=75.0%, RRR=4.21), DOHOME (Prob=69.7%, RRR=3.44)

### 2. BALANCED (เกณฑ์สมดุล)
```
Prob >= 58% AND RRR >= 1.5 AND AvgWin > 1.5% AND AvgLoss < 2.0% AND Count >= 10
```
- **ผลลัพธ์:** 6 หุ้น
- **ใช้:** Prob%, AvgWin%, AvgLoss%, RRR ครบทุกตัว
- **เหมาะกับ:** หุ้นที่สมดุลทั้ง Prob และ RRR

### 3. HIGH_PROB (เน้น Prob สูง)
```
Prob >= 65% AND RRR >= 1.2 AND AvgWin > AvgLoss AND Count >= 10
```
- **ผลลัพธ์:** 7 หุ้น
- **ใช้:** Prob%, AvgWin%, AvgLoss%, RRR
- **เหมาะกับ:** หุ้นที่ชนะบ่อย (Mean Reversion)

### 4. HIGH_RRR (เน้น RRR สูง)
```
RRR >= 2.0 AND Prob >= 55% AND AvgWin > 2.0% AND Count >= 10
```
- **ผลลัพธ์:** 2 หุ้น
- **ใช้:** Prob%, AvgWin%, RRR
- **เหมาะกับ:** หุ้นที่กำไรมาก (Trend Following)

### 5. QUALITY (คุณภาพดี)
```
Prob >= 60% AND AvgWin > 1.5% AND AvgLoss < 1.5% AND RRR >= 1.3 AND Count >= 10
```
- **ผลลัพธ์:** 15 หุ้น
- **ใช้:** Prob%, AvgWin%, AvgLoss%, RRR ครบทุกตัว
- **เหมาะกับ:** หุ้นที่มีคุณภาพดีทุกด้าน

### 6. MARKET_SPECIFIC (ตามตลาด)

#### THAI (Mean Reversion)
```
Prob >= 58% AND RRR >= 1.3 AND AvgWin > 1.0% AND AvgLoss < 2.0% AND Count >= 10
```
- **เน้น:** Prob และ AvgWin
- **ผลลัพธ์:** 18 หุ้น

#### US (Trend Following)
```
Prob >= 52% AND RRR >= 1.5 AND AvgWin > 1.5% AND AvgLoss < 2.5% AND Count >= 10
```
- **เน้น:** RRR และ AvgWin
- **ผลลัพธ์:** 1 หุ้น

#### CHINA (Mean Reversion)
```
Prob >= 55% AND RRR >= 1.3 AND AvgWin > 1.0% AND Count >= 10
```
- **เน้น:** Prob
- **ผลลัพธ์:** 0 หุ้น

#### TAIWAN (Trend Following)
```
Prob >= 52% AND RRR >= 1.4 AND AvgWin > 1.0% AND Count >= 10
```
- **เน้น:** RRR
- **ผลลัพธ์:** 0 หุ้น

## 📈 Mentor Score

**สูตรที่ใช้ Prob%, AvgWin%, AvgLoss%, RRR:**
```
Mentor_Score = (Prob% × 0.4) + (RRR × 15) + (AvgWin% × 2) - (AvgLoss% × 2)
```

**น้ำหนัก:**
- Prob%: 0.4 (ความถี่ชนะ)
- RRR: 15 (กำไรต่อความเสี่ยง)
- AvgWin%: 2 (กำไรเฉลี่ย)
- AvgLoss%: -2 (ขาดทุนเฉลี่ย - ลบออก)

**Top 5 หุ้น (ตาม Mentor Score):**
1. **PTTEP** (TH) - Score=99.3, Prob=75.0%, RRR=4.21, AvgWin=4.04%, AvgLoss=0.96%
2. **DOHOME** (TH) - Score=83.7, Prob=69.7%, RRR=3.44, AvgWin=2.95%, AvgLoss=0.86%
3. **TPIPP** (TH) - Score=63.7, Prob=68.8%, RRR=2.32, AvgWin=1.19%, AvgLoss=0.51%
4. **BCH** (TH) - Score=51.8, Prob=62.1%, RRR=1.69, AvgWin=1.97%, AvgLoss=1.17%
5. **CPF** (TH) - Score=51.7, Prob=67.8%, RRR=1.57, AvgWin=1.42%, AvgLoss=0.90%

## 💰 Risk Management (ใช้ AvgWin% และ AvgLoss%)

### 1. Stop Loss
```
Stop Loss = AvgLoss% × 1.5
```
- **เหตุผล:** เผื่อความผันผวน
- **ตัวอย่าง:**
  - PTTEP: AvgLoss=0.96% → Stop Loss=1.44%
  - DOHOME: AvgLoss=0.86% → Stop Loss=1.29%

### 2. Take Profit
```
Take Profit = AvgWin% × 0.8
```
- **เหตุผล:** รับกำไรก่อน (80% ของกำไรเฉลี่ย)
- **ตัวอย่าง:**
  - PTTEP: AvgWin=4.04% → Take Profit=3.23%
  - DOHOME: AvgWin=2.95% → Take Profit=2.36%

### 3. Position Size (ใช้ Prob% และ RRR)
```
Position_Size_Factor = (Prob% / 60%) × (RRR / 2.0)
Position_Size = Base_Size × (Position_Size_Factor / Max_Factor) × 3.0
```
- **Base Size:** 1% ของพอร์ต
- **Max:** 5% ต่อหุ้น
- **ตัวอย่าง:**
  - PTTEP: Prob=75%, RRR=4.21 → Position Size=3.0%
  - DOHOME: Prob=69.7%, RRR=3.44 → Position Size=1.5%

## 📊 สถิติผลลัพธ์

### ตาม Filter Type

| Filter Type | จำนวน | Prob% Mean | RRR Mean | AvgWin% Mean | AvgLoss% Mean |
|-------------|-------|-----------|----------|--------------|---------------|
| **STRICT** | 3 | 71.2% | 3.32 | 2.73% | 0.78% |
| **BALANCED** | 4 | 62.2% | 1.59 | 1.73% | 1.08% |
| **HIGH_PROB** | 4 | 67.1% | 1.44 | 2.33% | 1.73% |
| **QUALITY** | 9 | 62.0% | 1.35 | 1.76% | 1.30% |
| **THAI_MARKET** | 18 | 61.3% | 1.43 | 1.65% | 1.18% |
| **US_MARKET** | 1 | 57.1% | 1.61 | 1.84% | 1.14% |

### ตามประเทศ

| ประเทศ | จำนวน | Prob% Mean | RRR Mean | AvgWin% Mean | AvgLoss% Mean |
|--------|-------|-----------|----------|--------------|---------------|
| **TH** | 37 | 62.9% | 1.58 | 1.85% | 1.23% |
| **US** | 2 | 61.2% | 1.49 | 1.69% | 1.13% |

## 🎯 วิธีใช้งาน

### 1. เลือกเกณฑ์ตามความต้องการ

**ต้องการหุ้นดีมาก:**
- ใช้ **STRICT** (3 หุ้น) หรือ **HIGH_PROB** (7 หุ้น)

**ต้องการหุ้นสมดุล:**
- ใช้ **BALANCED** (6 หุ้น) หรือ **QUALITY** (15 หุ้น)

**ต้องการหุ้นตามตลาด:**
- ใช้ **MARKET_SPECIFIC** (37 หุ้น)

### 2. ใช้ Mentor Score เพื่อเรียงลำดับ

- เรียงตาม Mentor Score (สูงสุด = ดีที่สุด)
- ใช้ Prob%, AvgWin%, AvgLoss%, RRR ครบทุกตัว

### 3. ใช้ Risk Management

- **Stop Loss:** ตาม AvgLoss% × 1.5
- **Take Profit:** ตาม AvgWin% × 0.8
- **Position Size:** ตาม Prob% และ RRR

## 📂 ไฟล์ที่เกี่ยวข้อง

- `scripts/improved_filtering_mentor_approved.py` - สคริปต์กรองหุ้น
- `data/mentor_approved_symbol_selection.csv` - ผลลัพธ์ 39 หุ้น
- `data/symbol_performance.csv` - ข้อมูลพื้นฐาน

## ✅ สรุป

1. **ใช้ Prob%, AvgWin%, AvgLoss%, RRR ตามที่ Mentor ต้องการ** ✅
2. **มีหลายเกณฑ์ให้เลือก** (Strict, Balanced, High Prob, High RRR, Quality)
3. **มี Mentor Score** ที่ใช้ Prob%, AvgWin%, AvgLoss%, RRR
4. **มี Risk Management** (Position Size, Stop Loss, Take Profit)
5. **ได้หุ้นมากขึ้น:** 39 ตัว (vs 3 ตัวเดิม)

---

**วันที่สร้าง:** 2026-01-XX  
**ผู้สร้าง:** Stock Analysis System

